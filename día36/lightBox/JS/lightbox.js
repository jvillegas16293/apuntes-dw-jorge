const listaEnlaces = document.querySelectorAll(".a");
const lightbox = document.querySelector(".lightbox");
const cerrar = document.querySelector(".btnCerrar");
const imgBig = document.querySelector(" .imgBig");

// Rrecorre la lista de enlaces


// Agrega un evento click a cada enlace


// Agregar la clase actibe a lightbox

// Imgboig cambiarle el src por la imagen que tiene el enlace

// en caso de que haga click en cerrar, tengo que quitar la clase active a lightbox

// Si hago click e n cerrar..,. tengo que quitar la clase active a lightbox
